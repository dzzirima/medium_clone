
server:
  port: 4000
  error:
    include-message: always
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: fanset
    url: jdbc:postgresql://localhost:5432/funda
    username:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
